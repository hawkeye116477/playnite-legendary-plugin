<UserControl x:Class="LegendaryLibraryNS.LegendaryGameInstaller"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LegendaryLibraryNS"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:pcon="clr-namespace:Playnite.Converters"
             xmlns:pcmd="clr-namespace:Playnite.Commands"
             mc:Ignorable="d" d:DesignHeight="180" d:DesignWidth="600" Name="LegendaryGameInstallerUC" Loaded="LegendaryGameInstallerUC_Loaded">

    <UserControl.Resources>
        <pcon:NegateConverter x:Key="NegateConverter" />
        <sys:Boolean x:Key="True">True</sys:Boolean>
        <sys:Boolean x:Key="False">False</sys:Boolean>
    </UserControl.Resources>
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top">
            <Label Content="{DynamicResource LOCGameInstallDirTitle}"></Label>
            <DockPanel>
                <Button DockPanel.Dock="Right" Padding="5" Margin="5 0 0 0" Content="📁" Name="ChooseGamePath" Click="ChooseGamePath_Click"/>
                <Grid>
                    <TextBox Name="SelectedGamePathTxtBox" Text="" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="0,5"/>
                </Grid>
            </DockPanel>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0" VerticalAlignment="Bottom">
            <Button DockPanel.Dock="Bottom" x:Name="cancelButton" Content="{DynamicResource LOCCancelLabel}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,0" Click="CancelButton_Click" IsCancel="True"/>
            <Button DockPanel.Dock="Bottom" x:Name="importButton" Content="{DynamicResource LOCLegendaryImportEGL}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,5,0" Click="ImportButton_Click"/>
            <Button DockPanel.Dock="Bottom" x:Name="installButton" Content="{DynamicResource LOCInstallGame}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="InstallButton_Click" IsEnabled="False"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal" Name="DSizePanel">
                <Label Content="{DynamicResource LOCLegendaryDownloadSize}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,0,0,5"/>
                <Label x:Name="downloadSize" Content="{DynamicResource LOCDownloadingLabel}" VerticalAlignment="Top" Margin="5,0,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Name="ISizePanel">
                <Label Content="{DynamicResource LOCLegendaryInstallSize}"/>
                <Label x:Name="installSize" Content="{DynamicResource LOCDownloadingLabel}" VerticalAlignment="Top" Margin="5,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</UserControl>
